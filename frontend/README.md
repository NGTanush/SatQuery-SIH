# SatQuery Dashboard

Run the FastAPI backend, then start the React/Vite dashboard:

```bash
npm install
npm run dev
```

Set `VITE_API_URL` when the API is not running on `http://127.0.0.1:8000/api/v1`.
