# Training Run Records

Create one Markdown file per completed adaptation run, for example
`2026-08-29-rsvqa-adapter.md`. Record the dataset source and split, base model,
training command, hyperparameters, seed, metrics, checkpoint SHA-256, and
limitations. Do not store large model weights in this repository; place their
path or artifact-store URI in the run record.
